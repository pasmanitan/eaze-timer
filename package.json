{
  "name": "eaze-timer",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "clean": "rm -rf dist && echo 'cleaned!'",
    "dist": "bun clean && bun build ./src/index.html --minify --splitting --outdir ./dist && bun build ./src/assets/serviceWorker.js --minify --outdir ./dist/ && echo 'built prod files!'",
    "prod": "bun dist && bun run ./dist/index.html",
    "dev": "bun run ./src/index.html"
  }
}
